
const lc_searchInsert = (nums, target) => {
    // let left = 0, right = nums.length - 1
    // let index = 0
    // while(left < right) {
    //     const mid = parseInt((right + left) / 2)
    //     if (nums[mid] === target) {
    //         index = mid
    //         break
    //     }

    //     if (nums[mid] < target) {
    //         left = mid
    //     } else {
    //         right = mid
    //     }
    //     // index = mid
    // } 

    // return index
};

export const lc_searchInsertTest = () => {
    // console.log(lc_searchInsert([1,3,5,6], 5))
    // console.log(lc_searchInsert([1,3,5,6], 2))
    // console.log(lc_searchInsert([1,3,5,6], 7))
    // console.log(lc_searchInsert([1,3,5,6], 0))
}